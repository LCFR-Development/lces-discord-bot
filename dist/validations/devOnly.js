/* Optimized production build generated by CommandKit */
import {
  __name
} from "../chunk-4HQ2LG3N.js";

// src/validations/devOnly.ts
import { EmbedBuilder } from "discord.js";
function devOnly_default({ commandObj, interaction, handler }) {
  if (!commandObj.options)
    return false;
  const commandOptions = commandObj.options;
  if (!commandOptions.devOnly)
    return false;
  const noPermsEmbed = new EmbedBuilder().setDescription(
    `:x: You do not have permission to use this command:

This command is dev only`
  ).setColor("Red");
  if (!handler.devUserIds.includes(interaction.user.id)) {
    if (interaction.isChatInputCommand()) {
      interaction.reply({ ephemeral: true, embeds: [noPermsEmbed] });
    }
    return true;
  }
  return false;
}
__name(devOnly_default, "default");
export {
  devOnly_default as default
};
